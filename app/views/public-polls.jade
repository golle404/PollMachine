extends ./layout.jade

block content
	.container
		.row
			.content
				.row
					.content-header
						if polls
							h1 List of public polls you can vote to
						else
							p.lead.text-center Sorry, there are no public polls at the moment.
					.content-body
						if polls
							include ./poll-list.jade
